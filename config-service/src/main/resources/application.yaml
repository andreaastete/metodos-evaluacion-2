server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/andreaastete/metodos-evaluacion-2
          search-paths: config-data
  application:
    name: config-service



